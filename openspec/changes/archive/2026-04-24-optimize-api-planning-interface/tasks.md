## 1. 工具列表缓存与定时刷新

- [x] 1.1 实现 `ToolCache` 类，支持工具列表缓存和定时刷新
- [x] 1.2 实现 `refresh_tool_cache()` 函数，从 MCP Gateway 获取工具列表
- [x] 1.3 实现 `get_available_tools()` 函数，返回缓存的工具列表（自动刷新）
- [x] 1.4 在 `startup` 事件中初始化缓存并启动定时刷新任务
- [x] 1.5 实现 `periodic_tool_refresh()` 后台任务，每 5 分钟刷新一次

## 2. 异步任务执行模式

- [x] 2.1 修改 `/api_planning` 接口，生成 task_id 并保存到 MongoDB
- [x] 2.2 实现 `execute_task_async()` 函数，后台异步执行任务
- [x] 2.3 使用 `asyncio.create_task()` 启动异步任务，不阻塞响应
- [x] 2.4 立即返回 `{task_id, status: "pending"}` 给前端
- [x] 2.5 实现 `/api_task_status/{task_id}` 接口，返回任务状态（pending/running/completed/failed）

## 3. 简化的任务复杂度检测

- [x] 3.1 在 executor.py 中新增 `detect_task_complexity()` 函数（简化版，不包含 tools）
- [x] 3.2 实现任务复杂度检测的 prompt，只返回 `task_type` 和 `steps`
- [x] 3.3 实现返回值解析逻辑，提取 `task_type`、`steps` 字段
- [x] 3.4 添加判定失败时的 fallback 机制，默认返回 `task_type: "single"`
- [x] 3.5 添加日志记录，记录任务类型判定结果和耗时

## 4. 动态超时策略

- [x] 4.1 定义 `TIMEOUT_CONFIG` 配置（single: 30s, multi: 120s, video: 180s）
- [x] 4.2 在异步任务执行中根据任务类型获取超时时间
- [x] 4.3 使用 `asyncio.wait_for()` 设置超时
- [x] 4.4 处理超时异常，标记任务为 "failed"
- [x] 4.5 添加超时日志记录，包含任务类型和超时时间

## 5. MCP Gateway 工具调用集成

- [x] 5.1 修改 `generate_plan()` 函数，使用动态工具列表（从缓存获取）
- [x] 5.2 更新 LLM prompt，传入可用工具列表
- [x] 5.3 实现 `execute_tool_call()` 函数，调用 MCP Gateway 的 tools 接口
- [x] 5.4 添加工具调用的超时处理（默认 10 秒）
- [x] 5.5 实现工具调用结果整合逻辑，将结果注入 LLM 上下文

## 6. 日志与监控

- [x] 6.1 添加任务复杂度判定的统计日志（单任务/多任务比例）
- [x] 6.2 添加工具调用的统计日志（调用次数、跳过次数、失败次数）
- [x] 6.3 添加执行路径选择的统计日志（快速路径/完整路径比例）
- [x] 6.4 添加性能监控日志（各阶段耗时）
- [x] 6.5 添加错误日志，便于调试和问题排查

## 7. 测试与验证

- [ ] 7.1 测试单任务场景（简单问答），验证快速路径执行
- [ ] 7.2 测试多任务场景（需要工具调用），验证完整路径执行
- [ ] 7.3 测试重复工具调用场景，验证去重机制
- [ ] 7.4 测试 MCP Gateway 工具调用，验证路由和执行
- [ ] 7.5 测试工具调用失败场景，验证重试和降级
- [ ] 7.6 性能测试：对比优化前后的响应时间
- [ ] 7.7 边界测试：空输入、超长输入、特殊字符输入

## 8. 文档与部署

- [ ] 8.1 更新 API 文档，说明任务复杂度检测和工具调用机制
- [ ] 8.2 添加配置说明（如超时时间、重试次数等）
- [ ] 8.3 添加故障排查指南
- [ ] 8.4 部署到测试环境，进行集成测试
- [ ] 8.5 监控生产环境日志，验证功能正常
